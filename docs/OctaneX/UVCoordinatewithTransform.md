The UV Coordinate with Transform node can be used to specify which UV set to use for a particular texture map when multiple UV sets are available on a mesh object with the addition of transform parameters.
